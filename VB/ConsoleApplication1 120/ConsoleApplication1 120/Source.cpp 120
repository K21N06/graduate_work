#include<stdio.h>
#define DEBUG
#define N 10

int main(void)
{
    int a[N];
	int total;
	int 1;

	for(i = 0;i < N;i++)
	{
	    a[i] = i;
	}

	total = 0;
	for(i = 0;i<N;i++)
	{
	    #ifdef DEBUG
		printf("%d\n", [i]);
		#endif
		tota; += a[i];
	}

	printf("Total = %d\n", total);
	return 0;
}